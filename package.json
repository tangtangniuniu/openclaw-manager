{
  "name": "openclaw-manager",
  "version": "0.1.0",
  "private": true,
  "description": "A sleek, tech-forward control panel for OpenClaw — gateway control, config editor, Ollama model discovery, agent lab, skill workshop, and backup vault.",
  "license": "MIT",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "install:all": "npm install --workspaces --include-workspace-root=false",
    "dev": "npm-run-all --parallel dev:server dev:web",
    "dev:server": "npm --workspace server run dev",
    "dev:web": "npm --workspace web run dev",
    "build": "npm --workspace web run build && npm --workspace server run build",
    "start": "npm --workspace server run start",
    "test": "npm --workspace server test",
    "typecheck": "npm --workspace server run typecheck && npm --workspace web run typecheck"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
